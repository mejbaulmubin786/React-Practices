অবশ্যই! এবার আমি আপনার দেওয়া আলোচনার কাঠামো অনুসরণ করে পুরো বিষয়টি **ধাপে ধাপে, বিশদভাবে এবং উদাহরণসহ সুন্দরভাবে সাজিয়ে** দিচ্ছি যেন আপনি বা অন্য কেউ একদম শুরু থেকে React Component, Props এবং Dynamic Design সম্পর্কে পরিষ্কার ধারণা পেতে পারেন।

---

## 📘 React Components, Props ও Dynamic UI: একটি পূর্ণাঙ্গ ধাপে ধাপে গাইড

---

### 🔹 ধাপ ১: কমপোনেন্ট কী?

React-এ **কমপোনেন্ট** হলো একটি ফাংশন যা UI এর নির্দিষ্ট অংশ রেন্ডার করে। এটি HTML কোডের মতো দেখতে হলেও এটি JSX এর মাধ্যমে লেখা হয়। কমপোনেন্ট গুলো আমরা তৈরি করি যাতে করে **কোড রিপিট না করে বারবার ব্যবহার** করা যায়।

📌 **দুই প্রকার কমপোনেন্ট**:

1. **Stateless Functional Component**
2. **Class-based Component** (এখন কম ব্যবহৃত হয়)

আমরা এখানে ব্যবহার করবো **Stateless Functional Component**, যা একটি ফাংশনের মতো দেখতে।

---

### 🔹 ধাপ ২: একটি সিম্পল কমপোনেন্ট তৈরি (Title.jsx)

📁 ফাইল স্ট্রাকচার:

```
src/
 └── components/
     └── Title.jsx
```

🧾 **Title.jsx:**

```jsx
import React from 'react';

const Title = () => {
  return (
    <h1>This is a Title Component</h1>
  );
};

export default Title;
```

🧾 **App.jsx:**

```jsx
import React from 'react';
import Title from './components/Title';

const App = () => {
  return (
    <div>
      <Title />
    </div>
  );
};

export default App;
```

🧠 **বুঝুন:**

* `Title.jsx` ফাইলে আমরা কমপোনেন্ট তৈরি করেছি।
* `App.jsx` ফাইলে আমরা এটি **ইমপোর্ট করে ব্যবহার** করেছি।

---

### 🔹 ধাপ ৩: নতুন Component তৈরি (Card.jsx) - Static Version

```jsx
import React from 'react';

const Card = () => {
  return (
    <div className="card" style={{ width: '18rem' }}>
      <img src="..." className="card-img-top" alt="..." />
      <div className="card-body">
        <h5 className="card-title">Card Title</h5>
        <p className="card-text">Static text for card</p>
        <a href="#" className="btn btn-primary">Go somewhere</a>
      </div>
    </div>
  );
};

export default Card;
```

**App.jsx ফাইলে বারবার ব্যবহার:**

```jsx
<Card />
<Card />
<Card />
```

🧠 **সমস্যা:** সব Card একই তথ্য দেখাচ্ছে। আমরা তো আলাদা আলাদা টাইটেল, টেক্সট চাই!

---

### 🔹 ধাপ ৪: Props দিয়ে Dynamic Card

#### ✅ App.jsx

```jsx
<Card 
  title="Card 1" 
  text="This is Card 1" 
  btn="Visit Now" 
/>
<Card 
  title="Card 2" 
  text="This is Card 2" 
  btn="Learn More" 
/>
```

#### ✅ Card.jsx

```jsx
import React from 'react';

const Card = (props) => {
  return (
    <div className="card" style={{ width: '18rem' }}>
      <img src="..." className="card-img-top" alt="..." />
      <div className="card-body">
        <h5 className="card-title">{props.title}</h5>
        <p className="card-text">{props.text}</p>
        <a href="#" className="btn btn-primary">{props.btn}</a>
      </div>
    </div>
  );
};

export default Card;
```

#### ✅ আরও সহজ করতে Destructuring:

```jsx
const Card = ({ title, text, btn }) => {
  return (
    <div className="card" style={{ width: '18rem' }}>
      <img src="..." className="card-img-top" alt="..." />
      <div className="card-body">
        <h5 className="card-title">{title}</h5>
        <p className="card-text">{text}</p>
        <a href="#" className="btn btn-primary">{btn}</a>
      </div>
    </div>
  );
};
```

---

### 🔹 ধাপ ৫: Design (CSS) Props দিয়ে ক্লাস পরিবর্তন

#### ✅ App.jsx

```jsx
<Card 
  title="Card 1" 
  text="Card 1 Text" 
  btn="Click" 
  design="design-one"
/>
<Card 
  title="Card 2" 
  text="Card 2 Text" 
  btn="Go" 
  design="design-two"
/>
```

#### ✅ Card.jsx

```jsx
const Card = ({ title, text, btn, design }) => {
  return (
    <div className={`card ${design}`} style={{ width: '18rem' }}>
      <img src="..." className="card-img-top" alt="..." />
      <div className="card-body">
        <h5 className="card-title">{title}</h5>
        <p className="card-text">{text}</p>
        <a href="#" className="btn btn-primary">{btn}</a>
      </div>
    </div>
  );
};
```

#### ✅ index.css

```css
.design-one {
  background-color: lightyellow;
}

.design-two {
  background-color: lightblue;
}
```

---

## ✅ সংক্ষেপে প্রতিটি বিষয় মনে রাখার জন্য টিপস

| বিষয়            | কাজ                                           |
| --------------- | --------------------------------------------- |
| `Component`     | কোডের ছোট টুকরো, আলাদা ফাইল                   |
| `Props`         | Component-এ data পাঠানোর উপায়                 |
| `Destructuring` | props.title → title করে সহজে ব্যবহার          |
| `Dynamic Class` | Design বা style প্রপস দিয়ে ক্লাস নাম পাল্টানো |
| `Reusable`      | একবার কমপোনেন্ট লিখে বারবার ব্যবহার করা যায়   |

---

## 🧠 JSX এর কিছু বিশেষ নিয়ম

1. একাধিক HTML ট্যাগ return করতে চাইলে `<></>` বা `<div>` দিয়ে wrap করতে হবে।
2. `class` নয়, `className` লিখতে হয়।
3. `style={{}}` এর মাধ্যমে inline style দিতে হয়।
4. JSX-এ JavaScript expression লিখতে চাইলে `{}` ব্যাবহার করতে হয়।

---





























# ---------------------------


import React from 'react'


const App = () => {
  return (
    <div>App</div>
  )
}

export default App


এই সিনটেক্সটা তে রিটার্ন এর ভেতর সাধারণত আমরা এইচটিএমএল লিখি এবং রিটার্ন এর আগে const App = () => { এর পরে সাধারণত জাভাস্ক্রিপ্ট কোর্ড লিখা হয় ।  আজকে মুলত আমরা শিখবো কমপোনেন্ট । কমপোনেন্ট গুলো যাতে ছড়িয়ে ছিটিয়ে না থাকে তাই আমি components নামে একটি ফোল্ডার এসআরসি এর মাঝে তৈরি করে নিলাম। রিয়েক্ট এ ফোল্ডার এর নাম ছোট হাতের অক্ষরে হয়। এই ফোল্ডার এর ভেতরে আমি আমার প্রথম কম্পোনেন্ট হিসেবে Title.jsx নামে একটি ফাইল তৈরি করলাম । এই কম্পোনেন্ট ফাইল গুলোর প্রথম অক্ষর বড় হাতের হবে। এবার যে ফাইল থেকে আমি এটিকে ব্যবহার করতে চাই সেটিতে এটিকে ইমপোর্ট করতে হবে  import Title from './components/Title' এর মাধ্যমে এবং যে স্থানে দরকার সেখানে <Title /> এভাবে লিখবো ।  আমি এখন কার্ড নামে একটি নতুন কমপোনেন্ট তৈরি করলাম 
import React from 'react'

const Card = () => {
  return (
    <>
      <div className="card" style={{ width: '18rem' }}> {/* এখানে className এবং style ঠিক করা হয়েছে */}
        <img src="..." className="card-img-top" alt="..." /> {/* img ট্যাগে self-closing স্ল্যাশ যোগ করা হয়েছে */}
        <div className="card-body">
          <h5 className="card-title">Card title</h5>
          <p className="card-text">Some quick example text to build on the card title and make up the bulk of the card’s content.</p>
          <a href="#" className="btn btn-primary">Go somewhere</a>
        </div>
      </div>
    </>
  )
}

export default Card

এখন আমার যতো টি এই কার্ড দরকার ততবার <Card />
      <Card />
      <Card />
      <Card /> লিখবো। এখানে লক্ষ্য করলে একটি সমস্যা দেখা যাবে আমরা যতবার কাডটিকে কল করছি তত বারই একই লেখার কাড আসছে কিন্তু আমরা তো ভিন্ন ভিন্ন কার্ড চাইতেই পারি। এর জন্য আমরা যেখান থেকে কার্ড গুলো কল করছি সেখান থেকে প্রপস মানে প্রোপার্টি পাঠাতে পারি। ধরুন আমি এভাবে <Card title="Card-1"/> লিখলাম এখানে titleহচ্ছে প্রপস এবং Card-1 হচ্ছে তার ভেলু ।  এখন যেখানে আমরা কার্ডটি তৈরি করেছি  সেখানে const Card = (props) => { ব্যবাহর করে প্রপসটি রিসিভ করতে পারি এবং console.log(props.title) দিয়ে প্রপ্স রিসিভ করেছে কিনা তা টেস্ট করতে পারি। এখন মানগুলো রিসিভ করতে চাইলে <h5 className="card-title">{props.title}</h5> এভাবে করবো। এভাবে আগেরটির সাথে যদি আরো একটি পাঠাই <Card title="Card-1" text="Some quick example text"/> একই ভাবে রিসিভ করবো <h5 className="card-title">{props.title}</h5>
          <p className="card-text">{props.text}</p> এভাবে । একই ভাবে চাইলে বার্টন এর জন্য ও একটি প্রপ্সস পাঠাতে পারি । এখানে আমরা একটি বিষয় লক্ষ্য করলে দেখবো যে এখানে props ব্যবহার করায় বার বার props লিখতে হচ্ছে যেমন props.title, props.text, এভাবে অনেক অনেক বার লিখতে হতে পারে। এটিকে আমরা ডিসট্রাকচ্যার করে নিতে পারি । যেতেহু const Card = (props) => { তে সেকেন্ড ব্রাকেট তাই বলা যায় এটি একটি অবজেক্ট আমরা let {title, text}=props এভাবে লিখতে পারি মানে {title, text} এগুলো ডিসট্রাকচ্যার হবে props থেকে। এভাবে লিখলে আমরা আর {props.title} এভাবে না লিখে {title} এভাবে লিখলেই চলবে। আমরা আরো সহজ করে const Card = ({title, text}) => { এভাবে লিখতে পারি। যদটুকু এই পর্যন্ত দেখলাম এর মাধ্যমে আমরা এইছটিএমল  চেঞ্জ করতে পারবো কিন্তু আমাদের তো কোন একটি কার্ড এর স্টাইল ও চেঞ্জ করতে হবে পারে । ধরুন আমি ব্যাগগ্রাউন্ড কালার চেঞ্জ করতে চাই এখন এই ব্যাকগ্রাউন্ড কালার টা আছে কোথায় আমরা যেখানে কার্ডটি তৈরি করেছি সেখানে className="card" নামে একটি ক্লাস দিয়েছে এই ক্লাস কে ধরে আমি সিএসএস এ ডিজাইন করেছি তার মানে এই মেইন ক্লাসটি স্টেটিক কিন্তু আমরা এটিকে স্টেটিক রাখবো কেন? এখন এটিকে ডাইনামিক করার জন্য আমরা আরেকটি প্রপস পাঠাতে পারি সেটি হবে ডিজাইন এর জন্য। এভাবে <div className={design} <Card design="design-one"










          এই পর্যন্ত আমরা দুটি জিনস চেঞ্জ করলাম একটি হচ্ছে কনটেক্স আরেকটি হচ্ছে আরেকটি হচ্ছে স্টাইল এর সাথে আরেকটি বিষয় দরকার সেটি হচ্ছে টাইপ ধরুন আমরা যে বার্টনটি তৈরি করেছি সেটি একরকমের নাও হতে পারে কয়েক রকমের হতে পারে যদিও  এই কাজটি আমরা সিএসএস দিয়ে করতে পারি কিন্তু আমরা এখন টাইপ এর ব্যবহার দেখবো। ধরুন আমরা যে কার্ড বানিয়ে ছি কোন কার্ডে তো বার্টন নাও লাগতে পারে সেটি আমরা কি করে করবো। 
      